# 전체 회원 조회 — GET /users (인증 필요)
# token 헤더에 JWT 원문을 넣는다({{authToken}} = 02-auth-login 캡처값).

### 정상: 전체 회원 목록 → 200 OK
GET {{userHost}}/users
token: {{authToken}}

### 인증 실패: token 헤더 누락 → 403 Forbidden
GET {{userHost}}/users
